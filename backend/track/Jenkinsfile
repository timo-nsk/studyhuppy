pipeline {
    agent any

    environment {
        SERVICE = 'modul-service'
    }

    tools {
            maven "M3"
    }

    stages {
        stage('Führe Tests aus') {
            steps {
                dir('backend/track') {
                    echo "Starte Tests für ${SERVICE}"
                    bat 'mvn clean test'
                }
            }
        }
    }
}
