services:
  studyhuppy-frontend:
    container_name: studyhuppy-frontend
    build: .
    ports:
      - "9520:80" #siehe angular.json
    expose:
      - 80
    networks:
      - internalnet

networks:
  internalnet:
    driver: bridge
